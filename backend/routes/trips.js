const express = require('express');
const router = express.Router();

// Placeholder for trip routes - can be implemented later
router.get('/', (req, res) => {
  res.json({ message: 'Trip routes - to be implemented' });
});

module.exports = router;
